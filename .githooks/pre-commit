#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"

echo "[pre-commit] Formatting source files..."
source scripts/format.sh

echo "[pre-commit] Staging updates..."
git add .

echo "[pre-commit] Done."